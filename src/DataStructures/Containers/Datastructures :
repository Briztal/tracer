
All structures have dynamic capabilities;

Layer 0 : Resizeable Container : resizeable container of primitive types;

    - Can add / set / remove elements by index
    - Can access elements anywhere;
    - No size limitation;
    - reallocation management;


Layer 1 : Object Container : resizeable container of object pointers ;

    - Container of object pointers;
    - Takes creation and deletion in charge;

     - Can add / set / remove elements by index
     - Can access elements anywhere;
